# CineSentiment_RNN
This is a simple project implements a sentiment analysis model using a Simple RNN trained on the IMDB dataset. It features text preprocessing, model training, and an interactive Streamlit app for predicting movie review sentiment.
